<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tecdo.mac.sql2bot.mapper.MessageMapper">

    <resultMap id="BaseResultMap" type="com.tecdo.mac.sql2bot.domain.Message">
        <id column="id" property="id"/>
        <result column="conversation_id" property="conversationId"/>
        <result column="role" property="role"/>
        <result column="content" property="content"/>
        <result column="sql_query" property="sqlQuery"/>
        <result column="result_data" property="resultData"/>
        <result column="chart_config" property="chartConfig"/>
        <result column="error_message" property="errorMessage"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.tecdo.mac.sql2bot.domain.Message"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message (conversation_id, role, content, sql_query, result_data, chart_config, error_message)
        VALUES (#{conversationId}, #{role}, #{content}, #{sqlQuery}, #{resultData}, #{chartConfig}, #{errorMessage})
    </insert>

    <delete id="deleteById">
        DELETE FROM message WHERE id = #{id}
    </delete>

    <delete id="deleteByConversationId">
        DELETE FROM message WHERE conversation_id = #{conversationId}
    </delete>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM message WHERE id = #{id}
    </select>

    <select id="selectByConversationId" resultMap="BaseResultMap">
        SELECT * FROM message WHERE conversation_id = #{conversationId} ORDER BY created_at ASC
    </select>

    <select id="selectRecentMessages" resultMap="BaseResultMap">
        SELECT * FROM message
        WHERE conversation_id = #{conversationId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

</mapper>

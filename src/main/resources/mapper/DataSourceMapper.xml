<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tecdo.mac.sql2bot.mapper.DataSourceMapper">

    <resultMap id="BaseResultMap" type="com.tecdo.mac.sql2bot.domain.DataSource">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="host" property="host"/>
        <result column="port" property="port"/>
        <result column="database_name" property="databaseName"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="properties" property="properties"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.tecdo.mac.sql2bot.domain.DataSource"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO datasource (name, type, host, port, database_name, username, password, properties, status)
        VALUES (#{name}, #{type}, #{host}, #{port}, #{databaseName}, #{username}, #{password}, #{properties}, #{status})
    </insert>

    <delete id="deleteById">
        DELETE FROM datasource WHERE id = #{id}
    </delete>

    <update id="update" parameterType="com.tecdo.mac.sql2bot.domain.DataSource">
        UPDATE datasource
        SET name = #{name},
            type = #{type},
            host = #{host},
            port = #{port},
            database_name = #{databaseName},
            username = #{username},
            password = #{password},
            properties = #{properties},
            status = #{status}
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM datasource WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM datasource ORDER BY created_at DESC
    </select>

    <select id="selectByName" resultMap="BaseResultMap">
        SELECT * FROM datasource WHERE name = #{name}
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT * FROM datasource WHERE status = #{status} ORDER BY created_at DESC
    </select>

</mapper>
